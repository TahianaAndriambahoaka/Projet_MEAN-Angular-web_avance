import { SeparateurMillerPipe } from './separateur-miller.pipe';

describe('SeparateurMillerPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateurMillerPipe();
    expect(pipe).toBeTruthy();
  });
});
